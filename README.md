# DLL Proxy
Proxy for version.dll with x86 and x64 support

## Usage
* Install MSVC
* Add your logic to `DLLP_OnAttach` and `DLLP_OnDetach`
* Open MSVC x86 or x64 Developer Command Prompt
* Compile with `compile.cmd`
